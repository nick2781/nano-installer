name: CI

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  windows-verify:
    name: Windows Verify
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.81.0
          components: rustfmt

      - name: Restore cargo cache
        uses: Swatinem/rust-cache@v2

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Test library
        run: cargo test -p nano-installer-lib --lib

      - name: Test CLI
        run: cargo test -p nano-installer-cli

      - name: Check installer crates
        run: cargo check -p nano-installer-lib -p nano-installer-cli -p nano-installer-lzma -p uninst

      - name: Lint TapTap-v2 UI resources
        run: cargo run -p nano-installer-cli -- harness lint-resources --project examples/TapTap-v2 --format text

      - name: Build release stubs
        run: cargo build --release -p nano-installer-lzma -p uninst -p nano-installer-cli

      - name: Build TapTap-v2 setup
        run: cargo run --release -p nano-installer-cli -- build --project examples/TapTap-v2

      - name: Upload TapTap-v2 setup artifact
        uses: actions/upload-artifact@v4
        with:
          name: TapTapV2_Setup
          path: examples/TapTap-v2/dist/TapTapV2_Setup.exe
